{
  "name": "nominav2",
  "private": true,
  "description": "Cloudflare Pages estatico + Functions. Las Functions NO usan dependencias npm: pdf-lib se importa desde CDN (esm.sh) porque Pages no ejecuta npm install (sin build command). Este archivo NO declara dependencias a proposito.",
  "dependencies": {}
}
